version: '3'

services:

  git-server:
    image: mitchharvey/git-server-docker
    restart: always
    container_name: git-server
    ports:
      - "2222:22"
    volumes:
    - ~/git-server/keys:/git-server/keys
    - ~/git-server/repos:/git-server/repos

